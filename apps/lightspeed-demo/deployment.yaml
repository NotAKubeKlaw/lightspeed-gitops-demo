apiVersion: apps/v1
kind: Deployment
metadata:
  name: vulnerable-app
  namespace: lightspeed-demo
  labels:
    app: vulnerable-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vulnerable-app
  template:
    metadata:
      labels:
        app: vulnerable-app
    spec:
      containers:
        - name: web
          image: registry.access.redhat.com/ubi9/nginx-126:9.7-1775695204
          ports:
            - containerPort: 8080
